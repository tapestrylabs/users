on: [push]

jobs:
  test_job:
    permissions:
      contents: 'read'
      id-token: 'write'
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: 'actions/checkout@v3'
      - uses: dfalgout/test-fabric-gh@v0.1.7-test